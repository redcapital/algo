#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{
  int a0, b0, a1, b1, a2, b2;
  cin >> a0 >> b0 >> a1 >> b1 >> a2 >> b2;
  cout << (a0 - a2) << " " << (b0 - b1) << endl;
  return 0;
}
